{% extends 'base.html.twig' %}

{% block body %}
    <h1>Stanmagazynowy creation</h1>

    {{ form_start(form) }}
        {{ form_label(form.dostawca) }}
        {{ form_errors(form.dostawca) }}
        {{ form_widget(form.dostawca) }}
        <a href="{{ path('dostawca_new') }}">Dodaj dostawce (jezeli nie istnieje na liscie obok)</a>
        </br>
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('stanmagazynowy_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
