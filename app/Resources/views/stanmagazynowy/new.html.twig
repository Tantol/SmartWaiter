{% extends 'base.html.twig' %}

{% block body %}
    <h1>Stanmagazynowy creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}
    yyyy-MM-dd HH:mm:ss

    <ul>
        <li>
            <a href="{{ path('stanmagazynowy_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
