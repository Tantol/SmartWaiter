{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pozycja_zamowienium</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pozycja_zamowienium.id }}</td>
            </tr>
            <tr>
                <th>Ilosc</th>
                <td>{{ pozycja_zamowienium.ilosc }}</td>
            </tr>
            <tr>
                <th>Cena_jedn</th>
                <td>{{ pozycja_zamowienium.cenajedn }}</td>
            </tr>
            <tr>
                <th>Czas_przygotowania</th>
                <td>{{ pozycja_zamowienium.czasPrzygotowania }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('pozycja_zamowienia_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('pozycja_zamowienia_edit', { 'id': pozycja_zamowienium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
