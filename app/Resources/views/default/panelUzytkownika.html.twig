{% extends 'base.html.twig' %}

{% block title %}Panel uzytkownika{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header">
        <h3>Panel uzytkownika {{ user.username }}</h3>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-dark">
                <tbody>
                    <tr>
                        <th>Zmien hasło</th>
                        <td>
                            <a href="{{ path('userChangePassword', { 'id': user.id }) }}">Zmien</a>
                        </td>
                    </tr>
                    <tr>
                        <th>Zmien email (obecny email - {{ user.email }})</th>
                        <td>
                            <a href="{{ path('userEditEmail', { 'id': user.id }) }}">Zmien</a>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div class="card-footer">
    </div>
</div>
{% endblock %}

