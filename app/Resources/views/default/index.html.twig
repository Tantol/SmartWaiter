{% extends 'base.html.twig' %}

{% block title %}Strona startowa{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header">
        <h3>Wszystkie funkcje:</h3>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-dark">
                <tbody>
                  <tr>
                      <td>
                          <a href="{{ path('login') }}">Logowanie <- dodac captcha</a>
                      </td>
                  </tr>
                  <tr>
                      <td>
                          <a href="{{ path('register') }}">Rejestracja <- dodac captcha</a>
                      </td>
                  </tr>
                  <tr>
                      <td>
                          <a href="{{ path('danie_index') }}">Komponowanie dań<- skonczone podstawowa wersja, potrzebny tylko front !</a>
                      </td>
                  </tr>
                  <tr>
                      <td>
                          <a href="{{ path('zamowienie_index') }}">Zamowienia <- skonczone podstawowa wersja, potrzebny tylko front !</a>
                      </td>
                  </tr>
                  <tr>
                      <th><a href="{{ path('adminPanel') }}">Panel administratora <- skonczone</a></th>
                  </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div class="card-footer">
    </div>
</div>
{% endblock %}

