{% extends 'base.html.twig' %}

{% block body %}
    <h1>Rodzaj</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rodzaj.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ rodzaj.nazwa }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('danie_new') }}">Back to the new danie</a>
        </li>
        <li>
            <a href="{{ path('rodzaj_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('rodzaj_edit', { 'id': rodzaj.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
