{% extends 'base.html.twig' %}

{% block body %}
    <h1>Status_zamowienium</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status_zamowienium.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ status_zamowienium.nazwa }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('status_zamowienia_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('status_zamowienia_edit', { 'id': status_zamowienium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
